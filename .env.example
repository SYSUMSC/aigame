# 共享 Compose 环境变量
# 说明：`docker-compose.yml`、`docker-compose.dev.yml`、`docker-compose.deploy.yml`
# 都会优先读取根目录 `.env` 中的这些变量。
#
# 使用方式：
# 1. 复制为根目录 `.env`
# 2. 按需修改端口、密码、公开访问地址

# 时区
TZ=Asia/Shanghai

# 对外访问主机名 / 域名
# 开发环境可保持 localhost，服务器部署时可改为你的域名。
PUBLIC_HOST=localhost

# MongoDB
MONGO_ROOT_USERNAME=root
MONGO_ROOT_PASSWORD=password
MONGODB_REPLICA_SET=rs0
MONGO_HOST_PORT=37017

# Redis
REDIS_PASSWORD=password
REDIS_HOST_PORT=36379

# MinIO
MINIO_ROOT_USER=root
MINIO_ROOT_PASSWORD=password
MINIO_API_HOST_PORT=39000
MINIO_CONSOLE_HOST_PORT=39001

# 应用对外端口
WEBAPP_HOST_PORT=33000
EVALUATEAPP_HOST_PORT=38000
